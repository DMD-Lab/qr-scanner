import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // --- Couleur primaire (Electric Indigo) ---
  static const primary = Color(0xFF6366F1);
  static const primaryLight = Color(0xFF818CF8);
  static const primaryDark = Color(0xFF4F46E5);

  // --- Neutres Dark ---
  static const darkBgPrimary = Color(0xFF0F0F0F);
  static const darkBgSecondary = Color(0xFF1A1A1A);
  static const darkBgTertiary = Color(0xFF252525);
  static const darkBorder = Color(0xFF2E2E2E);
  static const darkTextPrimary = Color(0xFFF5F5F5);
  static const darkTextSecondary = Color(0xFFA0A0A0);
  static const darkTextDisabled = Color(0xFF555555);

  // --- Neutres Light ---
  static const lightBgPrimary = Color(0xFFFFFFFF);
  static const lightBgSecondary = Color(0xFFF5F5F5);
  static const lightBgTertiary = Color(0xFFEBEBEB);
  static const lightBorder = Color(0xFFE0E0E0);
  static const lightTextPrimary = Color(0xFF0F0F0F);
  static const lightTextSecondary = Color(0xFF666666);
  static const lightTextDisabled = Color(0xFFBBBBBB);

  // --- Sémantiques ---
  static const successDark = Color(0xFF4CAF82);
  static const successLight = Color(0xFF2E7D52);
  static const warningDark = Color(0xFFF5A623);
  static const warningLight = Color(0xFFC47D0A);
  static const errorDark = Color(0xFFE05353);
  static const errorLight = Color(0xFFB52B2B);
  static const infoDark = Color(0xFF5B9CF6);
  static const infoLight = Color(0xFF2563EB);
}
